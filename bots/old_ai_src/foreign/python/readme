
python xpilot-ai interface.  To you this you must have the xpilot-ai c library from xpilot-ai.org


To build python xpai:

python setup.py build
cp build/lib*/xpai.so .


Look at pytest.py for a demo.  type "python pytest.py" to run it.
